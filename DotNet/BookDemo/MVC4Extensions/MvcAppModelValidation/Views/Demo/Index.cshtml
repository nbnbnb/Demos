@{
    ViewBag.Title = "Index";
}

@model ModelValidator[]

<h2>Index</h2>

<table>
    @for (int i = 0; i < Model.Length; i++)
    {
        <tr>
            <td>@(i + 1)</td>
            <td>@Model[i].GetType().Name</td>
        </tr>
    }
</table>